# P6: HTTPRoute derived 1:1 from live Ingress monitoring/kube-prometheus-stack-grafana (hosts + backend parity).
# Legacy Ingress stays until P7 commit 3.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - name: teyvat-gateway
      namespace: envoy-gateway-system
  hostnames:
    - grafana.local
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
