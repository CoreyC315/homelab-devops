apiVersion: v1
kind: Service
metadata:
  name: suwayomi
  namespace: default
  annotations:
    metallb.io/loadBalancerIPs: 192.168.1.52
spec:
  selector:
    app: suwayomi
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4567
