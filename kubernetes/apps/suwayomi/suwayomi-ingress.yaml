apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: suwayomi-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: suwayomi.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: suwayomi
                port:
                  number: 4567
