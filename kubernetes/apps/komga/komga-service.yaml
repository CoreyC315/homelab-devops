apiVersion: v1
kind: Service
metadata:
  name: komga
  namespace: default
spec:
  selector:
    app: komga
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 25600
