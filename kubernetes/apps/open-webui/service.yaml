apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: ai-stack
spec:
  selector:
    app: open-webui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
