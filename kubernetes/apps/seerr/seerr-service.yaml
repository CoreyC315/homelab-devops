apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: default
  annotations:
    metallb.io/loadBalancerIPs: 192.168.1.53
spec:
  selector:
    app: seerr
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5055
