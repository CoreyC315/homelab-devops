apiVersion: v1
kind: Service
metadata:
  name: vllm-chat
  namespace: ai-stack
  labels:
    app: vllm-chat
    app.kubernetes.io/part-of: vllm
spec:
  selector:
    app: vllm-chat
  ports:
    - name: http
      port: 8000
      targetPort: http
