apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelable
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/CoreyC315/homelab-devops.git"
    path: kubernetes/apps/homelable
    targetRevision: HEAD
  destination:
    server: "https://kubernetes.default.svc"
    namespace: homelable
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
